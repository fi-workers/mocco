# Committed shared defaults (no secrets). Personal/secret overrides go in the
# gitignored `env/.env`, which wins over this file (see infra/local/scripts/with-env.ts).

# Local browsing: https://www.mocco.work (traefik+mkcert, hosts). prod: https://www.mocco.club (ADR 0006)
SERVICE_DOMAIN=www.mocco.work

DATABASE_URL=postgres://mocco:mocco@localhost:5432/mocco
