services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: mocco
      POSTGRES_PASSWORD: mocco
      POSTGRES_DB: mocco
    ports: ['5432:5432']
    volumes: ['mocco_pg:/var/lib/postgresql/data']
volumes:
  mocco_pg:
