node_modules/
.DS_Store
*.log
dist/
build/
.next/
.turbo/
coverage/
.env
.env.*
!.env.example

# packages/frontend/env/ — committed defaults + template; env/.env stays ignored.
!packages/frontend/env/.env.local
!packages/frontend/env/.env.example
.yarn/*
!.yarn/patches
!.yarn/releases

next-env.d.ts
*.tsbuildinfo
infra/local/cert/*.pem

# Local agent/editor settings (never commit to the public repo)
# — except .claude/skills/, which is shared contributor tooling.
.claude/*
!.claude/skills/
CLAUDE.local.md

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/
